<!DOCTYPE html>
<html lang="en">
{{> head}}
<body>
    {{#if message}}
    <div class="message">{{message}}</div>
    {{/if}}
    {{#if isLogedIn}}
    <a href="/auth/logout">Logout</a>
    {{else}}
    <a href="/auth/login">Login</a>
    <a href="/auth/register">Register</a>
    {{/if}}
    <h1>This is private page, <strong>{{username}}</strong>, thats you are logged in!</h1>
</body>

</html>