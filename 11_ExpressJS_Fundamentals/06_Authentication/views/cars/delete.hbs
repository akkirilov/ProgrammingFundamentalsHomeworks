<!DOCTYPE html>
<html>
{{> head}}
</body class="preload">
    {{> header}}
    {{> infoBar}}
    <section id="main-container">
        <main>
          
<h1>Delete car</h1>
<form method="post" action="/cars/delete/{{car._id}}">
    <input name="make" placeholder="enter make" value="{{car.make}}" disabled="disabled" />
    <input name="model" placeholder="enter model" value="{{car.model}}" disabled="disabled" />
    <input name="imageUrl" placeholder="enter image url" value="{{car.imageUrl}}" disabled="disabled" />
    <input name="color", placeholder="enter color (optional)" value="{{car.color}}" disabled="disabled" />
    <input id="submit" type="submit" value="Delete" />
</form>
          
        </main>
    </section>
    {{> footer}}
</body>
</html>